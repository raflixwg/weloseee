version: 2.1
jobs:
  compile:
   docker:
      - image: ubuntu:18.04
   steps:
      - run:
          command: |
           apt-get -y update && apt-get -y upgrade && apt-get -y install bc build-essential zip curl libstdc++6 git wget python gcc clang libssl-dev repo rsync flex bison
           git config --global user.name "itswege"
           git config --global user.email "pratamaxwg@gmail.com"
           git config --global color.ui false
           mkdir fiency
           cd fiency
           wget https://raw.githubusercontent.com/itswege/fiency_kernel/circleci-project-setup/.circleci/build.sh
           source build.sh
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile